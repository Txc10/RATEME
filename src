import React from "react";
import ReactDOM from "react-dom/client";
import RateMeApp from "./Copy of RATEME (Prototype)";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RateMeApp />
  </React.StrictMode>
);
